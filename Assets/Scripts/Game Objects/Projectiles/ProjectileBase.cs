using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
///  Base class for a projectile bullet, interacts with objects that are iBreakable or iDamagable when collided, and destroyed when colliding an iBulletObstacle
/// </summary>
public class ProjectileBase : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
